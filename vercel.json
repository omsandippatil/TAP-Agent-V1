{
  "builds": [
    {
      "src": "app/main.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": ["app/static/**", "app/render/templates/**", "app/render/assets/**", "config.yaml"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/app/main.py" }
  ]
}
